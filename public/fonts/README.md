Place font files here for the custom "A Little Bit FancAAAY" font.

Steps:

1. Download the font files you have rights to from the source (e.g. the page you linked).
2. Put the files in this folder and name them exactly:
   - `alittlebitfancaaay.woff2`
   - `alittlebitfancaaay.woff` (optional fallback)
3. Restart the dev server. The app's global CSS references these files and will use the font when present.

Notes:

- Ensure the font license allows web embedding. If unsure, ask or use a licensed copy.
- If you want me to fetch and add the font files, grant explicit permission and confirm license compliance.
